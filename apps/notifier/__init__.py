"""Reference application under test: a release notifier that posts to Slack.

This package must not import Invariant harness types, scoring keys,
observer records, or fault-schedule enums. It sees only adapter-visible results.
"""
